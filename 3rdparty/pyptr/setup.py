from skbuild import setup

if __name__ == "__main__":
    setup(
        name = 'pyptr',
        packages = ['pyptr'],
        version = '0.0.1',
        description = 'Simple weakref extension',
        license = 'MIT',
    )

